module.exports = function jsonp(api) {

  return function(req, res, next) {
    
    // jsonp support
    // @todo check in query for correct callback variable
    res.api.content = (req.api.params.callback || 'callback') + '(' + res.api.content + ');';  
    next();
  }

}